# Rails Chrome Webdriver

This image contains:
- Ruby + cached Rails 7 dependencies (from the default Gemfile)
- Chrome + ChromeDriver 114 (version string is in environment variable `CHROMEDRIVER_VERSION`)
